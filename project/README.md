# Single Page - HTML Only
***Put your project files in this folder.***

## Goal:
Students will create a single webpage on a topic of your choice with headers, paragraphs, text formatting, links, and text formatting.

Students will code an HTML page (`index.html`) and use markup to structure a page on a topic of your choice.

The page should have a minimum of 3 sections each with a section header, paragraphs, links, and lists. In one or more of the sections, the student needs to create at least two different lists each with at least 3 list items.

## Requirements
### Folder and file structure requirements
* Create a single web page named `index.html`

### HTML requirements
* A single web page (HTML file)
* All standard HTML5 required tags (`DOCTYPE`, `html`, `head`, `title`, `body`)
* A title (using the proper tag)
* An `h1` tag for your title (there can be only one `h1` tag per page)
* At least 3 `h2` tags for your section headers (subtitles)
* A minimum of 6 `p` tags (2 for each section)
* At least 2 lists (`ul` or `ol`) with at least 3 list items per list
* At least 3 links to external websites
* A variety of text formatting tags such as `strong`, `em`, `code`, `cite`
    * for bold and italic text, be sure to pick good keywords to emphasize.
    * a proficient score will include at least 4 examples of these tags beign properly used.

### Validity Requirements
* No HTML errors (this will be tested in the html tests)

### CSS Recommendations
NOTE: for this assignment, it is not required, but it will run tests on your CSS for those wishing to know they are on the right track:

* A Google font or a Google font pairing.
* Background color set on the page and at least 1 other color set for text.
* Make sure any hyperlink is readable.
* Any color or background color set on an element passes the AIM color contrast checker
    - Must pass at a AAA rating

## Course Outcomes and Look Fors
### Outcome #1: HTML & CSS - the code
* I can build a web page using HTML and CSS that is clean, organized, and looks good on screen.
* I have a minimum of the HTML project requirements.

### Outcome #4: Design
* I can choose colors and fonts through CSS that make my website easy to read and nice to look at.
* My colors meet the WebAIM color contrast checker at a AA rating for Normal text.
* I chose proper HTML tags and coded them so that my website content is easy to read and user friendly.
* I can apply a readable Google font with CSS, and it doesn't conflict with the content of my site.

### Outcome #5: Standard Tools & Practices
* I can place my web content in the proper folder and use standard file names.
* I can track my progress through multiple git commits with meaningful commit messages.
* I have commit history showing up in GitHub Classroom.


### Assignment Submission Requirements
* Student must commit and push their changes to the repo (repo has the latest work)
* Layout should not look broken at any width
* No horizontal scrollbars should appear unless the window is more narrow that a single figure
* Student must record their image gallery at various viewport widths (or zoom levels) - best will go towards a video showing the browser being set to different screen widths.